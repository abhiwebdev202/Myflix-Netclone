version: '3'

services:
  web:
    image: myflix2:latest
    container_name: myflixapp_web
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./Myflix-Postgresql/netclone/Myflix/Myflixproject:/myflixapp
